/**
 * Public API of the auth bounded context. Other modules may import ONLY from this file.
 * Auth currently exposes no facade: nothing else needs to call into it.
 */
export { AuthModule } from './auth.module.js';
